// $Id: kmisc.h,v 1.3 2002/08/22 20:42:36 Administrator Exp $

#ifndef _kmisc_h_
#define _kmisc_h_

/*
#define GETTYPE(n) get_string( get_hash( (n), "type" ) )
#define GETSUBTYPE(n) get_string( get_hash( (n), "subtype" ) )
#define MUSTTYPE(n,type) \
  do { if (NE( GETTYPE((n)), (type) )) \
    kerr( "Bad node type: %s!=%s", get_string( get_hash( (n), "type" ) ), type ); \
  } while (0)

#define EQ(s0,s1) (!strcmp((s0),(s1)))
#define NE(s0,s1) (strcmp((s0),(s1)))

#define ISTYPE(n,type) (EQ(GETTYPE((n)),(type)))
*/

#endif /* _kmisc_h_ */
